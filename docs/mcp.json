{
  "servers": {
    "grumpydev-mcp": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "genaiscript",
        "mcp",
        "--remote",
        "https://github.com/sinedied/grumpydev-mcp",
        "--remote-branch",
        "main",
        "--groups",
        "mcp"
      ],
      // "envFile": "${workspaceFolder}/.env"
    }
  }
}
